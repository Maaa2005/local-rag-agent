# 完全ローカル型 事務用自律AIエージェント 要件定義書（フェーズ1/MVP版）

## 1. プロジェクト概要
### 1.1 目的
企業の機密情報（社内規定、議事録、顧客データ等）を外部APIに送信することなく、完全ローカル環境で学習・検索・回答できる高セキュリティな自律型AIエージェントを構築する。

### 1.2 コアバリュー
- **情報の非対称性解決**: ユーザー権限に応じた厳格な情報アクセス制御（Access Level Filtering）。
- **自律的学習**: 指定された共有フォルダを常時監視し、ユーザー操作なしに最新情報をバックグラウンドで自動学習。
- **完全閉域網動作**: インターネット接続なしでも機能し、データ流出リスクをゼロにする。

### 1.3 本書のスコープ
本書はフェーズ1（MVP）の要件を定義する。フェーズ2以降の AD/LDAP 連携、SaaS コネクタは §6 に概要のみ記載する。

---

## 2. アーキテクチャ要件
### 2.1 デプロイメント構成（ハイブリッド方式）
- **パターンA（個人・少人数向け）**: Tauri / Electron を用いたデスクトップアプリ化。
- **パターンB（チーム・全社向け）**: Docker Compose を用いた社内共通サーバー / プライベートクラウドへのデプロイ。

### 2.2 テクノロジースタック
| レイヤ | 採用技術 | 補足 |
|---|---|---|
| 推論エンジン | vLLM | PagedAttention によるスループット確保。バックグラウンドワーカーの一時停止と組み合わせて GPU リソースを制御 |
| 推論 LLM（主候補） | **Gemma 4（試作段階）** | Google 公開の次世代 OSS モデル。現時点では試作段階であり、本格運用前に社内ベンチで継続評価する |
| LLM フォールバック | Qwen2.5-7B-Instruct / Llama-3.1-Swallow-8B | Gemma 4 が試作段階のため、性能・ライセンス・公開条件の確定までの保険として推論ラッパーを抽象化し即時切替可能とする |
| LLM 量子化 | **INT4（AWQ または GPTQ）必須** | 12GB VRAM 環境では量子化なしでは載らないため、量子化を要件として固定する |
| Embedding モデル | `intfloat/multilingual-e5-large` を第一候補 | 比較対象として `cl-nagoya/ruri-large` を社内ベンチで評価し、日本語事務文書に対する Recall@10 で決定 |
| ベクトル DB | Qdrant | Dense + Sparse のハイブリッド検索を採用（§3.5） |
| 状態・タスク管理 | SQLite（WAL モード） | 外部ミドルウェア非依存 |
| パースエンジン | Docling + openpyxl / pandas | Docling のレイアウト AI は CPU 実行（§4.1 参照） |

### 2.3 Gemma 4 採用にあたっての扱い
- Gemma 4 は**試作段階**のため、ライセンス条文・ベンチ性能・公開タイミングが変動する可能性がある。
- 本プロジェクトでは Gemma 4 を**第一候補としつつ、社内検証で以下基準を満たした場合に正式採用**とする:
  - 日本語事務文書 RAG での回答正答率 80% 以上
  - Function Calling / JSON 構造化出力の妥当率 95% 以上
  - ライセンスが社内利用・改変・再配布要件に適合
- 上記が満たされない場合はフォールバックモデルへ即時切替できるよう、**モデル抽象化レイヤ（共通推論インターフェース）**を実装段階から用意する。

---

## 3. 機能要件
### 3.1 ユーザー認証と権限管理（Access Control）
#### パターンA（デスクトップ版）
- アプリ起動時に **Windows SID（Mac は UID + Directory Services UUID）** を取得し、SQLite の権限テーブルと照合する。
- アカウント名による照合は**禁止**（ローカルアカウント作成によるなりすまし回避）。
- SQLite 権限 DB はアプリ実行ユーザーから読み取り専用とし、書き込みは管理者署名付きの設定ファイル経由でのみ反映する。
- **既知の限界**: デスクトップ版はクライアント側で動作するため、OS 管理者権限を持つユーザーによる SQLite 改ざんを完全には防げない。本構成は「業務 PC を会社が IT 統制下に置く前提」での運用を想定する。本格的な権限分離が必要な場合はパターンB を選択するよう提案書に明記する。

#### パターンB（サーバー版）
- Web ブラウザ経由のアクセス時に ID / パスワード認証を提供し、SQLite の権限テーブルと照合する。
- セッションは HttpOnly / Secure Cookie、CSRF トークン、レート制限を実装する。
- フェーズ2 で AD / LDAP / OIDC 連携に拡張可能なよう、認証層をプラガブルに設計する。

#### 共通
- すべての検索で、ユーザーの `access_level` に基づき **Qdrant のプレフィルタリング**を行う。
- `access_level` は Qdrant の payload index に登録し、フィルタ評価コストを定数時間に抑える。

### 3.2 自律監視・データ収集機能（Watcher）
- 監視対象: ローカルディレクトリおよびネットワーク共有（SMB / NAS）。
- **監視方式**: ローカルは OS ネイティブイベント（inotify / FSEvents / ReadDirectoryChangesW）、SMB / NAS は**ポーリング方式（既定 5 分間隔、設定可能）**。SMB マウントで inotify が機能しないことを前提に、ポーリングをデフォルトとする。
- 差分判定: ファイルサイズ + 更新時刻による一次フィルタ → SHA-256 ハッシュによる確定判定。
- 変更タイプごとの処理:
  - **追加**: タスクキューに `INGEST` ジョブ登録。
  - **更新**: 旧チャンク群を `is_active = False` に更新後、新バージョンを `INGEST`。
  - **削除**: 該当ファイル由来の全チャンクを `is_active = False` に更新し、Qdrant からも論理削除（`deleted_at` 付与 → 定期バッチで物理削除）。

### 3.3 データパース・正規化機能
- **Excel**: `openpyxl` で結合セルを解除し同値補完。表形状（縦持ち / 横持ち / 帳票）を判定して `pandas`（データ型）または `Docling`（ドキュメント型）にルーティング。
- **PDF / Word**: `Docling` でレイアウト・表構造を保持した Markdown に変換。
- **画像のみ PDF**: フェーズ1 では OCR は対象外（フェーズ2 で PaddleOCR 等を検討）。

### 3.4 非同期処理・リソース制御（Task Queue）
- SQLite（WAL モード）をハブとした自己完結型タスクパイプライン。
- パイプライン: `PARSE → CHUNK → METADATA_EXTRACT(LLM) → QA_GENERATE(LLM) → EMBED → INDEX`
- 各フェーズで状態を保存し、リトライ時の**冪等性**を担保（ジョブ ID + フェーズ単位の `completed` フラグ）。
- **GPU リソース制御**:
  - ユーザーのチャット入力検知時、バックグラウンドの **Embedding / メタデータ抽出 / QA 生成ワーカーを一時停止**（in-flight ジョブは完了まで待機 → 新規投入を停止）。
  - vLLM の推論モデル本体は常駐ロードを維持する（モデル再ロードによるレイテンシ回避）。
  - 設計意図は「**バックグラウンド GPU 占有を停止して KV キャッシュ余裕を確保する**」ことであり、モデル自体をアンロードする方式ではない。

### 3.5 検索機能（ハイブリッド検索）
- **Dense ベクトル**: e5-large による意味検索。
- **Sparse ベクトル**: Qdrant のネイティブ Sparse（BM25 相当）を併用。
- **融合**: Reciprocal Rank Fusion（RRF, k=60）で統合。
- **権限フィルタ**: `access_level <= user.access_level` を Qdrant のプレフィルタとして適用。
- **Reranker**: フェーズ1 では未搭載（評価結果により BGE-Reranker をフェーズ1.5 で追加検討）。

### 3.6 QA 生成のスループット制約
- 全文書に対する LLM ベース QA 生成は計算コストが高い（7B INT4 / RTX 4070 Ti 12GB 想定でチャンク 1 件あたり数秒〜十数秒）。
- 初期投入時の所要時間目安を事前見積もりして提示（例: 1 万チャンクで概ね 8〜24 時間）。
- 運用上は QA 生成を**オプション機能**として ON/OFF 可能とし、軽量運用時は Dense + Sparse のみで稼働できるようにする。

---

## 4. 非機能要件
### 4.1 ハードウェア・リソース最適化制約
- **対象環境**: NVIDIA GeForce RTX 40 / 50 シリーズ（VRAM 12GB 〜 16GB 想定）。
- **VRAM 割り当て目安（12GB ケース）**:
  | 用途 | サイズ概算 |
  |---|---|
  | 推論 LLM（7B INT4 量子化） | 約 5〜6 GB |
  | vLLM KV キャッシュ | 約 2〜3 GB |
  | Embedding（e5-large、必要時のみロード） | 約 2 GB |
  | 余裕枠 | 約 1〜2 GB |
- **CPU オフロード方針**: Docling のレイアウト認識 AI、OCR（将来）など**バックグラウンド処理の重い計算は CPU 実行**を既定とする。ただし CPU 実行による Docling のスループット低下（PDF 1 ページあたり数秒〜数十秒）を許容する。
- **VRAM 自動最適化**: 起動時に VRAM 容量を検知し、12GB 環境では 7B 量子化モデル、16GB 環境では 14B 量子化モデルを自動選択する。Gemma 4 のサイズ展開に応じて選択ロジックを更新する。

### 4.2 情報の鮮度管理とハルシネーション低減
- 旧バージョンのチャンクは `is_active = False` でフィルタ除外し、**RAG が古い文書を参照することを防止**する。
- LLM 自体が事前学習由来の古い情報を生成する可能性は残るため、「物理的に防止」ではなく「**RAG コンテキスト経由のハルシネーションを抑止**」と位置付ける。
- 回答生成時はプロンプトで「提供された文書にない情報は『情報なし』と回答せよ」と明示する。

### 4.3 性能目標（フェーズ1）
- 初回応答レイテンシ: 中央値 5 秒以内（チャンク 10 件取得 + 7B INT4 生成）。
- Watcher 検知遅延: ローカル 1 秒以内、SMB 5 分以内（ポーリング間隔依存）。
- 初期インデックス構築: 1 万チャンクを 24 時間以内（QA 生成 OFF 時は 4 時間以内）。

### 4.4 セキュリティ
- 全通信は localhost / 社内 LAN に閉じる。外部 HTTP リクエストはホワイトリスト方式でブロック。
- ログには PII / 機密情報を含めない（マスキング処理）。
- フェーズ2 で監査ログ（誰がいつ何を検索したか）の永続化を実装。
- **アプリ分離によるデータガバナンス境界**: 本アプリは社内機密 RAG 専用で**ローカル LLM（vLLM）に固定**する。外部送信を伴うフロンティア LLM（Anthropic / OpenAI / Gemini / Claude Code / Codex 等）は**別アプリ `frontier-llm-agent` に物理分離**し、本アプリのプロバイダ registry には登録しない。これにより社内文書が外部 API へ流出する経路を設計レベルで遮断する（同一プロセス内に外部送信プロバイダもエージェントのツール実行環境も存在しない）。フロンティア LLM は社内文書を一切マウントしない別環境で汎用知識のみを扱う。

---

## 5. 評価基準（フェーズ1 リリース判定）
| 項目 | 合格基準 |
|---|---|
| 権限フィルタの正答率 | 100%（権限外文書が応答に含まれないこと） |
| 日本語事務文書 RAG 正答率 | 社内ベンチセット 50 問で 80% 以上 |
| Function Calling / JSON 出力妥当率 | 95% 以上 |
| 初回応答レイテンシ中央値 | 5 秒以内 |
| 24 時間稼働時の OOM 発生回数 | 0 件 |
| Gemma 4 評価判定 | 上記基準を満たさない場合はフォールバックモデルでリリース |

---

## 6. 今後の拡張（フェーズ2 以降）
- Active Directory / LDAP / OIDC 連携による認証統合（サーバー版）。
- SharePoint / Microsoft 365（Graph API）コネクタ。
- Google Drive / Box 等の SaaS コネクタ。
- BGE-Reranker などのリランカー統合。
- 画像 PDF 向け OCR パイプライン（PaddleOCR 等）。
- 監査ログ・操作ログの永続化と可視化ダッシュボード。
- Gemma 4 正式版リリース後のモデル更新・再評価。
