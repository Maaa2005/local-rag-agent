fastapi==0.115.6
uvicorn[standard]==0.32.1
aiosqlite==0.20.0
qdrant-client==1.12.1
openai==1.57.0
sentence-transformers==3.3.1
docling==2.18.0
openpyxl==3.1.5
pandas==2.2.3
tabulate>=0.9.0
watchdog==6.0.0
# python-jose はメンテ停止・既知CVE(CVE-2024-33663/33664)のため PyJWT に置換
PyJWT==2.10.1
passlib[bcrypt]==1.7.4
# passlib 1.7.4 は bcrypt 5.x と非互換 (__about__ 属性が無く起動時バグ検出が失敗)
bcrypt==4.0.1
python-multipart==0.0.20
pydantic-settings==2.6.1
httpx==0.28.1
# 注: 外部 LLM プロバイダ SDK (anthropic / google-genai / boto3 / claude-agent-sdk / mcp) は
# フロンティア専用アプリ frontier-llm-agent へ分離。本アプリはローカル LLM (vLLM) 固定のため不要。
# vLLM へは OpenAI 互換 API (上記 openai パッケージ) で接続する。
